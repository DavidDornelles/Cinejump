import React, { Fragment } from "react";
const Movie = (props: any) => {
  return (
    <Fragment>
      <div id='menu' style={{backgroundColor: 'red', height: '100px'}}>Menu</div>
    </Fragment>
  );
};

export default Movie;