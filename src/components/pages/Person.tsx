import React, { Fragment } from "react";

const Person = (props: any) => {
  return (
    <Fragment>
      <div style={{ backgroundColor: 'cyan', height: '100px'}}>Menu</div>
    </Fragment>
  );
};

export default Person;