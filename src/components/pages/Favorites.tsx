import React, { Fragment } from "react";
const Favorites = (props: any) => {
  return (
    <Fragment>
      <div style={{backgroundColor: 'white', height: '100px'}}>Menu</div>
    </Fragment>
  );
};

export default Favorites;